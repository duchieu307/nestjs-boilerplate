export enum AUTH_STRATEGY {
  LOCAL = 'local',
  JWT_AUTH = 'jwt-auth',
  JWT_REFRESH = 'jwt-refresh',
  API_KEY = 'api-key',
  BASIC_AUTH = 'basic-auth',
}

export enum HEADER_KEY {
  API_KEY = 'x-api-key',
}
